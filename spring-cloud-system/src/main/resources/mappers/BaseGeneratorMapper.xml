<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blade.system.mapper.BaseGeneratorMapper">
  <select id="selectTableList" resultType="com.blade.system.common.vo.TableVO">
    SELECT
      TABLE_NAME as tableName,
      TABLE_COMMENT AS tableComment
    FROM infomation_schema.TABLES t
    left join infomation_schema.TABLE_CONSTRAINTS tc on tc.TABLE_SCHEMA = t.TABLE_SCHEMA and tc.TABLE_NAME = t.TABLE_NAME
    where t.TABLE_SCHEMA = #{dataBase}
  </select>
</mapper>